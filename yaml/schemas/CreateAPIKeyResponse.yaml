type: object
properties:
  data:
    type: object
    properties:
      token:
        type: string
      client_id:
        type: string
      webhook_secret:
        type: string
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
  status:
    type: object
    properties:
      errors:
        type: array
        items:
          type: string
      message:
        type: string
